[build]
builder = "NIXPACKS"

[phases.setup]
nixPkgs = ["nodejs-22_x", "php82", "php82Extensions.exif"]

[deploy]
php artisan config:clear && php artisan cache:clear && php artisan route:clear && php artisan view:clear && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT